{
    "description": "Instance IDs of the BIG-IP Next instances to be backed up.",
    "properties": {
        "instance_ids": {
            "items": {
                "format": "uuid",
                "type": "string"
            },
            "type": "array"
        }
    },
    "required": [
        "instance_ids"
    ],
    "type": "object"
}